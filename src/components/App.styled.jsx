import styled from '@emotion/styled';

export const Container = styled.div`
  display: grid;

  position: relative;
  z-index: 0;

  grid-template-columns: 1fr;
  grid-gap: 16px;
  padding-bottom: 24px;
`;
